# Helper Scripts for Creating Jobs

## Job Naming

The jobs will be named uniformly according one or more templates.
Currently, only one template is supported. This template for job names
is `Task-SystemNodesGpusTime-Lang[-Spec]`, where

  * `Task` is prototypically `train/convert/diag/vocab/transl`  
  * `System` is `L/P/R/M` (referring to LUMI, Puhti ,Roihi, Mahti)
  * `NodesGpus` are of the form `1n1g`, `4n16g`, etc
  * `Time` is in minutes, hours, days: 10m, 1h, 2d
  * `Lang` is a :-separated list of short language codes `en:es`
  * `Spec` is a free specifier.

Example: `train-L-1n1g10m-en:es-test`

## Creating a Job Directory

The script `mkjob.sh` is used to change the job name, to make the
corresponding directory if it does not exist, and to add standard
details to the directory if it misses them.  The command takes one
command-line argument: the job name.
```
sh mkjob.sh train-L-1n1g10m-en:es-test
```
This is what you get:
```
making a job directory under $PROJHOME/data/
=================================
JOB_NAME   : train-L-1n1g10m-en:es-test
JOB_TASK   : train
JOB_SYSTEM : lumi
JOB_NODES  : 1
JOB_GPUS   : 1
JOB_TIME   : 0-00:10:00
JOB_LANG   : en:es
JOB_SPEC   : test
=================================
train-L-1n1g10m-en:es-test
├── base -> /project/project_462000964/members/aylijyra/base
├── cfg
│   └── sbatch-entry.slurm
├── in
│   ├── data
│   ├── models
│   └── vocab
├── logs
│   ├── slurm
│   └── tb
└── out
    ├── checkpoints
    ├── metrics
    ├── models
    └── translations
15 directories, 1 file
=================================
```

## Listing all job directories
```
sh showjobs.sh
```

## Creating the SBATCH file

If you set PROJHOME and `export JOB_NAME=train-L-1n1g10m-en:es-test`,
you can continue using other scripts to furnish the job directory.
For example, `sh mksbatch.sh` will prepare you a new sbatch file
called `cfg/sbatch-entry.slurm.new`.  It contains the directives, the
guards, the executable command/options, and a handy visualisation of
the allocated resources (to help avoid over-allocations).

```
#!/usr/bin/env bash
# AUTOGENERATED from cfg/params.sh with command:'
#     cd /project/project_462000964/members/aylijyra
#     export  JOB_NAME=train-L-1n1g10m-en:es-test
#     base/mammoth-helper/helper/bin/create  mksbatch.sh
# This is account agnostic (no -A option or #SBATCH --account)
# Instead, put into your .profile:
#     export SBATCH_ACCOUNT="project_462000964"

# ---- sbatch directives ----------------------------------------------------
#SBATCH --job-name=train-L-1n1g10m-en:es-test
# Tip: keep account out of file/CLI with: export SBATCH_ACCOUNT="$ACCOUNT"
#SBATCH --output=logs/%x-%j.out
#SBATCH --error=logs/%x-%j.err
#SBATCH --partition=dev-g
#SBATCH --nodes=1
#SBATCH --ntasks-per-node=1
#SBATCH --cpus-per-task=8
#SBATCH --gpus-per-task=1
#SBATCH --gres=gpu:mi250:1
#SBATCH --time=0-00:10:00
#SBATCH --mem=30G

# ---- guards for stopping accidental heavy jobs-----------------------------
export GUARD_MAX_NODES=4                       # do not change unless you need to
export GUARD_TIME="0-01:00:00"  # do not change unless you need to
# examples: GUARD_TIME="12:00:00" (12h) or GUARD_TIME="2-00:00:00" (2 days).

# ---- job payload (adjust as needed) ---------------------------------------
export JOB_PATTERN=slurm
export JOB_NAME=train-L-1n1g10m-en:es-test
export JOB_TASK=train
export JOB_SYSTEM=lumi
export JOB_NODES=1
export JOB_GPUS=1
export JOB_TIME=0-00:10:00
export JOB_LANG=en:es
export JOB_SPEC=test
# export JOB_SCRIPT="..."
# export JOB_ARGS="..."
source base/mammoth-helper/helper/bin/slurm/sbatch-tail.sh

# ---- visualization for a quick review "is this sensible?" -----------------
# ┌──────────┐
# │ Task 0   │
# │ GPU  0   │
# │ CPU 0–7  │
# └──────────┘
# On LUMI: 7 CPUs per GPU → 4 GPUs use 28 CPUs; the other 4 GPUs can still be scheduled.
# If you want to own the node, export JOB_EXCLUSIVE=1 (or JOB_CPU_SPLIT_POLICY=used) and take more.
```
